package com.omp.dev.test.action;

import com.omp.commons.util.CipherAES;

public class CipherAESTest {

    public void enc() {
        /*
        try {
            String formStr = "1234567123456";
            
            CipherAES cipher = new CipherAES();
            String str = cipher.encrypt(formStr);
            System.out.println("enc from[" +  formStr + "] to[" + str + "]");
            System.out.println("dec from[" +  str + "] to[" + cipher.decrypt(str) + "]");
            
        } catch (Exception e) {
            e.printStackTrace();
        }
        */
    }
    
    
}
